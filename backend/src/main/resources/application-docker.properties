spring.application.name=backend

# PostgreSQL
spring.datasource.url=jdbc:mariadb://mariadb-db:3306/step_quest
spring.datasource.username=${DOCKER_DB_PASSWORD}
spring.datasource.password=${DOCKER_DB_USER}
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# ORM
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true